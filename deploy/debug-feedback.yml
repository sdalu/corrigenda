# Install as /usr/local/etc/debug-feedback.yml. ../run reads this file
# too, so there is one config and no second copy to drift.

# Where reports are written. Whoever starts ../run must be able to write
# here, and Apache never touches it.
store: /var/db/debug-feedback

# Listened on by default. Apache proxies to it (see
# macro-debug-feedback.conf); ../run -p PORT overrides it for a run you
# want to reach with a browser directly.
socket: /var/run/debug-feedback/debug-feedback.sock

# A report naming a site outside this list is refused, so the endpoint
# cannot be used as free storage. Remove the key to accept any site;
# ../run -f drops it for that run, since the fixture is on no vhost.
sites:
    - tools.sdalu.com
    # Every site that mounts the endpoint (Use DebugFeedbackEndpoint in
    # its vhost) has to be listed here too: the vhost decides who may
    # post, this decides what the report may claim to be about.
    - www.alux.fr
